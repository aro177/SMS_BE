# Intentionally kept inert so `docker compose up` uses the same production-safe
# configuration as docker-compose.yml. Add personal development overrides in an
# untracked compose file and pass it explicitly with an additional `-f` option.
services:
  studentmanagementsystem: {}
